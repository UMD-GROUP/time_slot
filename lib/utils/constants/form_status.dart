enum ResponseStatus { pure, inProgress, inSuccess, inFail }

enum ValidationStatus { pure, valid, notValid }

enum OrderStatus { created, inProgress, done, cancelled }

enum PurchaseStatus { created, inProgress, finished, cancelled }
